# Gleam Test Runner

Run Gleam tests with proper error handling and output formatting.

```
gleam test
```

This command:
- Compiles the project if needed
- Runs all tests in the `test/` directory
- Shows detailed output for failures
- Validates type safety during compilation

Use this for TDD workflows and continuous testing.