# Gleam Build and Run

Build and execute the project on the BEAM VM.

```
gleam build && gleam run
```

This command:
- Compiles to Erlang VM bytecode
- Runs the project entrypoint
- Shows compilation errors if any
- Demonstrates BEAM VM execution

Use for testing CCL parsing and object construction.