# Gleam Check and Format

Type check the project and ensure proper formatting.

```
gleam check && gleam format
```

This command:
- Performs type checking without building
- Formats all Gleam source files
- Ensures code follows Gleam conventions
- Validates pattern matching exhaustiveness

Use this before commits to maintain code quality.